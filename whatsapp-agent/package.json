{
  "name": "day-lanches-whatsapp-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Day Lanches — agente de atendimento automatico via WhatsApp (VPS), integrado a Evolution API por webhook",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
